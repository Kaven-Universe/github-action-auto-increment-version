name: "Update file"
description: "Update file"
inputs:
  dir:
    default: ""
  file:
    default: ""
  index:
    default: -1
  increment:
    default: 1
  debug:
    default: false
outputs:
  oldVersion:
    description: "The old version"
  newVersion:
    description: "The new version"
runs:
  using: "node16"
  main: "dist/index.js"
